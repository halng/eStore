import { SideBar, TableView, ActionHeader } from '.'

describe('components', () => {
    test('exports SideBar', () => {
        expect(SideBar).toBeDefined()
    })

    test('exports TableView', () => {
        expect(TableView).toBeDefined()
    })

    test('exports ActionHeader', () => {
        expect(ActionHeader).toBeDefined()
    })
})
